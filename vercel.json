{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
          "maxLambdaSize": "50mb"
        }
      }
    ],
    "env": {
      "DATABASE_URL": "@your-database-url"
    },
    "buildCommand": "npm run build && npx prisma generate"
  }
  